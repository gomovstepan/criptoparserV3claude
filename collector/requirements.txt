# ── Web framework ──
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# ── База данных ──
asyncpg>=0.29.0

# ── Message Bus ──
redis>=5.0.0

# ── Криптобиржи (CCXT Pro входит в ccxt как ccxt.pro) ──
ccxt>=4.3.0

# ── Производительность ──
uvloop>=0.19.0
orjson>=3.10.0

# ── Логирование ──
structlog>=24.1.0

# ── Метрики ──
prometheus-client>=0.20.0

# ── Валидация / конфигурация ──
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
